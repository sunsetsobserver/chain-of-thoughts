{
  "task": "Compose one airy monophonic 3/4 bar (ticks 0..11) for SIX instruments in one go.",
  "bar_index": $BAR_INDEX,
  "ticks_in_bar": $BAR_TICKS,
  "meter": {"numerator": $NUM, "denominator": $DEN},
  "instruments": $INSTRUMENTS_JSON,
  "constraints": {
    "events_per_instrument": [3, 6],
    "time_strictly_increasing": true,
    "no_time_overlap": true,
    "bar_bounded": true,
    "velocity_steps_max": 8,
    "pitch_bounded_circularity": true,
    "duration_mul_div_two_only": true
  },
  "output_expectation": "Return ONE bundle JSON exactly per schema; six features (one per instrument) + run_plan."
}